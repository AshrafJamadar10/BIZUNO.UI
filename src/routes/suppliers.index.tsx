import { useState } from "react";
import { createFileRoute } from "@tanstack/react-router";
import { Pencil, Plus, Truck } from "lucide-react";
import { toast } from "sonner";
import { AppShell } from "@/components/layout/AppShell";
import { PageHeader } from "@/components/common/PageHeader";
import { SearchInput } from "@/components/common/SearchInput";
import { StatusBadge } from "@/components/common/StatusBadge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { useCreateSupplier, usePurchaseOrders, useSuppliers, useUpdateSupplier } from "@/hooks/queries/suppliers";
import { formatCurrency, formatDate } from "@/utils/format";
import { Pagination } from "@/components/common/Pagination";

export const Route = createFileRoute("/suppliers/")({ component: SuppliersPage });
const EMPTY = { name: "", contactPerson: "", phone: "", email: "", gstin: "", city: "", status: "active" as "active" | "inactive" };
function SuppliersPage() {
  const [search, setSearch] = useState(""); const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(10); const [open, setOpen] = useState(false); const [editingId, setEditingId] = useState<string | null>(null); const [form, setForm] = useState(EMPTY);
  const { data } = useSuppliers({ search, page, pageSize }); const { data: orders } = usePurchaseOrders({ pageSize: 50 }); const create = useCreateSupplier(); const update = useUpdateSupplier();
  const set = (key: keyof typeof EMPTY, value: string) => setForm((current) => ({ ...current, [key]: value }));
  return <AppShell><PageHeader title="Suppliers & Purchasing" description="Manage vendor relationships, purchase orders and incoming stock." crumbs={[{ label: "Home", to: "/" }, { label: "Suppliers & Purchasing" }]} actions={<Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button><Plus className="size-4" /> New supplier</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>{editingId ? "Edit supplier" : "Add supplier"}</DialogTitle></DialogHeader><div className="grid gap-3 sm:grid-cols-2">{(["name", "contactPerson", "phone", "email", "gstin", "city"] as const).map((key) => <div key={key}><Label>{key.replace(/[A-Z]/g, (m) => ` ${m}`).replace(/^./, (m) => m.toUpperCase())}</Label><Input className="mt-1.5" value={form[key]} onChange={(e) => set(key, e.target.value)} /></div>)}</div><DialogFooter><Button variant="outline" onClick={() => { setOpen(false); setEditingId(null); }}>Cancel</Button><Button disabled={!form.name || create.isPending || update.isPending} onClick={() => { const onSuccess = () => { toast.success(editingId ? "Supplier updated" : "Supplier added"); setOpen(false); setEditingId(null); setForm(EMPTY); }; if (editingId) update.mutate({ id: editingId, input: form }, { onSuccess }); else create.mutate(form, { onSuccess }); }}>{editingId ? "Save changes" : "Save supplier"}</Button></DialogFooter></DialogContent></Dialog>} /><Card className="p-0"><div className="border-b p-4"><SearchInput value={search} onChange={setSearch} placeholder="Search suppliers, city or contact" /></div><div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Supplier</TableHead><TableHead>Contact</TableHead><TableHead>City</TableHead><TableHead className="text-right">Outstanding</TableHead><TableHead>Status</TableHead><TableHead /></TableRow></TableHeader><TableBody>{data?.rows.map((supplier) => <TableRow key={supplier.id}><TableCell><p className="font-medium">{supplier.name}</p><p className="text-xs text-muted-foreground">{supplier.gstin ?? "GSTIN not added"}</p></TableCell><TableCell>{supplier.contactPerson}<p className="text-xs text-muted-foreground">{supplier.phone}</p></TableCell><TableCell>{supplier.city}</TableCell><TableCell className="numeric text-right">{formatCurrency(supplier.outstanding)}</TableCell><TableCell><StatusBadge status={supplier.status} /></TableCell><TableCell className="text-right"><Button variant="ghost" size="icon" aria-label={`Edit ${supplier.name}`} onClick={() => { setEditingId(supplier.id); setForm({ name: supplier.name, contactPerson: supplier.contactPerson, phone: supplier.phone, email: supplier.email, gstin: supplier.gstin ?? "", city: supplier.city, status: supplier.status }); setOpen(true); }}><Pencil className="size-4 text-muted-foreground" /></Button></TableCell></TableRow>)}</TableBody></Table>  </div><Pagination page={page} pageSize={pageSize} total={data?.total ?? 0} onPageChange={setPage} onPageSizeChange={setPageSize} /></Card><Card className="p-0"><div className="border-b p-4"><h2 className="font-semibold">Purchase orders</h2></div><div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Order</TableHead><TableHead>Supplier</TableHead><TableHead>Issued</TableHead><TableHead>Expected</TableHead><TableHead className="text-right">Total</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{orders?.rows.map((order) => <TableRow key={order.id}><TableCell className="font-medium">{order.number}</TableCell><TableCell>{order.supplierName}</TableCell><TableCell>{formatDate(order.issuedAt)}</TableCell><TableCell>{formatDate(order.expectedAt)}</TableCell><TableCell className="numeric text-right">{formatCurrency(order.total)}</TableCell><TableCell><StatusBadge status={order.status} /></TableCell></TableRow>)}</TableBody></Table></div></Card><div className="flex items-center gap-2 text-sm text-muted-foreground"><Truck className="size-4" /> Purchase receiving and stock reconciliation are ready to connect to the backend inventory ledger.</div></AppShell>;
}
